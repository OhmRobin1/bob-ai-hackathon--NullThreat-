# =============================================================================
# HACKATHON SUBMISSION METADATA
# =============================================================================
# Instructions:
#   - Fill in ALL required fields (marked with # REQUIRED)
#   - Optional fields can be left as empty strings ""
#   - Do NOT rename this file — the evaluation pipeline reads it by name
#   - Use double quotes around all string values
# =============================================================================

team:
  name: "NullThreat"                             # REQUIRED — e.g., "Orion Squad"
  track: "AI"                            # REQUIRED — one of: AI | DevOps | Sustainability | Open
  lead:
    name: "Bhargav Paragkumar Jagtap"                           # REQUIRED — e.g., "Alice Chen"
    email: "26cs031@charusat.edu.in"                          # REQUIRED — e.g., "alice.chen@ibm.com"
  members:
    - name: "Saumya Jain"                         # REQUIRED — Team member 1
      email: "26cs032@charusat.edu.in"
    - name: "Milonee Shah"                         # Add more members by copying the two lines above
      email: "26cs115@charusat.edu.in"
    - name: "Ohm Soman"
      email: "26cs119@charusat.edu.in"

submission:
  title: "NullThreat"                            # REQUIRED — Project display name, e.g., "SmartOps Dashboard"

  problem_statement: >                 # REQUIRED — 1-3 sentences. What problem? Who has it?
    Defence analysts face thousands of alerts from different sources,
    making it difficult to identify genuine threats while filtering out false positives.
    The solution prioritizes critical threats and generate clear BLUF (Bottom Line Up Front)
    summaries so commanders can quickly understand the situation and take action.

  solution_summary: >                  # REQUIRED — 1-3 sentences. What did you build?
    Replace this text with a clear description of your solution
    and how it addresses the problem above.

  key_features:                        # REQUIRED — List 3 to 5 specific features you implemented
 - **Alert Classification:** 40 ingested alerts triaged into confirmed threats vs. false positives, with a live signal-to-noise breakdown
- **MITRE ATT&CK Mapping:** Confirmed threats mapped across 10 ATT&CK techniques (e.g., T1071 C2 Protocol, T1041 Exfiltration, T1190 Exploit App), with a clickable technique matrix
- **Incident Correlation:** Related alerts grouped into incident clusters by shared technique, surfacing multi-alert attack chains
- **BLUF Summary Report:** Auto-generated Bottom Line Up Front report highlighting confirmed threat count, severity, and the most significant correlated cluster
- **Interactive Live Demo:** Paste or type any alert description in-browser and get an instant classification, with sample alerts and analysis history
- **Dual Classification Modes:** Offline 25-rule keyword engine by default; switches to IBM watsonx.ai for real zero-shot LLM classification via `CLASSIFY_MODE=watsonx`
                             # optional 5th

  tech_stack:
    languages: [Python,Javascript, HTML, CSS]                      # REQUIRED — e.g., ["Python", "TypeScript"]
    frameworks: [/CSS]                     # e.g., ["FastAPI", "React"]
    ibm_technologies: [IBM Bob, IBM watsonx.ai (Llama-3-70b-Instruct)]               # e.g., ["watsonx.ai", "IBM Bob", "IBM Cloud Functions"]
    databases: [None — alerts and results stored as CSV/JSON files]                      # e.g., ["PostgreSQL", "Redis"]
    other: [GitHub Pages (live demo hosting), GitHub Actions (template validation)]                          # e.g., ["Docker", "GitHub Actions", "Slack API"]

  what_we_are_most_proud_of: >         # Optional but recommended — guides judges to your best work
  We're proud that BLUFShield doesn't just classify alerts — it correlates them into incident clusters and grounds every confirmed threat in a real MITRE ATT&CK technique, giving analysts a framework-based starting point instead of a vague flag. We also built a genuinely interactive live demo where anyone can paste an alert and see the classification pipeline work in real time, rather than just showing static screenshots of output.
  known_limitations: >   
                # Optional Uses a simulated 40-alert dataset, not a live SIEM/satellite feed integration
  Demo mode classification uses a 25-rule keyword engine, not the LLM — watsonx.ai mode requires an API key to activate real zero-shot classification
  MITRE ATT&CK mapping is limited to 10 predefined techniques, not the full framework
  No persistent database — results are generated per run, not stored historically across sessionsbut recommended — be honest about gaps
   

# =============================================================================
# ARTIFACT LOCATIONS
# These paths are relative to the repo root. Only change if you moved files.
# =============================================================================
artifacts:
  source_code: "src/"
  setup_guide: "docs/setup-guide.md"
  architecture_doc: "docs/architecture.md"
  demo_video: "demo/demo-video-link.txt"   # URL to hosted video (YouTube, Loom, Box)
  live_demo: "demo/live-demo-url.txt"      # URL to deployed demo (optional)
  screenshots: "demo/screenshots/"
  presentation: "presentation/"
